/**
 * Shared prompt-builder helpers.
 *
 * Spec 01 placeholder. Spec 03 fills this in with the transferable prose
 * blocks (Tone helpers, Brainlift Operating Protocols, Tools Protocol, User
 * Context, Available Repo Skills) that both `buildAlphaXSystemPrompt` and
 * `buildBrainliftSystemPrompt` compose. Keeping the file present in Spec 01
 * locks the directory shape so spec 03 only adds content, not structure.
 */

export {};
